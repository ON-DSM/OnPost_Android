<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.yongjincompany.onpost">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Onpost.Splash"
        tools:targetApi="n">
        <activity
            android:name=".ui.member.UserInfoActivity"
            android:exported="false" />
        <activity
            android:name=".ui.post.PostDetailActivity"
            android:exported="false" />
        <activity
            android:name=".ui.post.SearchPostActivity"
            android:exported="false" />
        <activity
            android:name=".ui.post.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>